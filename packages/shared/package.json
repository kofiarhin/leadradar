{
  "name": "@leadradar/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, schemas, and constants shared by the LeadRadar client and server.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.5.1"
  }
}
